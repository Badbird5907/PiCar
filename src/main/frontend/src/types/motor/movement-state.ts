export enum MovementState {
    FORWARD = "FORWARD",
    BACKWARD = "BACKWARD",
    STOPPED = "STOPPED"
}

export enum MovementDirection {
    FORWARD = "FORWARD",
    BACKWARD = "BACKWARD",
    LEFT = "LEFT",
    RIGHT = "RIGHT",
    STOPPED = "STOPPED"
}